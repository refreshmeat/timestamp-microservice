# Timestamp Microservice

freeCodeCamp Back End Development and APIs project.

## API

- `GET /api/:date?`
- Empty date returns the current time.
- Accepts valid date strings and Unix timestamps.
- Invalid dates return `{"error":"Invalid Date"}`.
